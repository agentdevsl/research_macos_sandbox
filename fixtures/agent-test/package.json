{
  "name": "agent-test",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Test agent for sandbox validation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "sdk-test": "tsx src/sdk-test.ts",
    "claude-code-test": "claude --version && claude -p 'Say OK' --max-turns 1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@anthropic-ai/claude-agent-sdk": "^0.2.12"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0"
  }
}
